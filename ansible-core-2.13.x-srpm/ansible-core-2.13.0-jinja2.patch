diff -ur ansible-core-2.13.0rc1.orig/lib/ansible_core.egg-info/requires.txt ansible-core-2.13.0rc1/lib/ansible_core.egg-info/requires.txt
--- ansible-core-2.13.0rc1.orig/lib/ansible_core.egg-info/requires.txt	2022-05-02 10:46:54.000000000 -0400
+++ ansible-core-2.13.0rc1/lib/ansible_core.egg-info/requires.txt	2022-05-07 05:48:04.421924081 -0400
@@ -1,4 +1,6 @@
-jinja2>=3.0.0
+# Causes RHEL issues
+#jinja2>=3.0.0
+jinja2
 PyYAML
 cryptography
 packaging
diff -ur ansible-core-2.13.0rc1.orig/requirements.txt ansible-core-2.13.0rc1/requirements.txt
--- ansible-core-2.13.0rc1.orig/requirements.txt	2022-05-02 10:45:40.000000000 -0400
+++ ansible-core-2.13.0rc1/requirements.txt	2022-05-07 05:47:08.092746577 -0400
@@ -3,7 +3,11 @@
 # packages.  Thus, this should be the loosest set possible (only required
 # packages, not optional ones, and with the widest range of versions that could
 # be suitable)
-jinja2 >= 3.0.0
+#
+# Cauuses RHEL compilaiton issues
+#jinja2 >= 3.0.0
+jinja2
+
 PyYAML
 cryptography
 packaging
--- ansible-core-2.13.0.orig/lib/ansible/cli/__init__.py	2022-05-16 10:56:46.000000000 -0400
+++ ansible-core-2.13.0/lib/ansible/cli/__init__.py	2022-05-17 21:33:05.709743488 -0400
@@ -23,11 +23,16 @@
 # Used for determining if the system is running a new enough Jinja2 version
 # and should only restrict on our documented minimum versions
 jinja2_version = version('jinja2')
-if jinja2_version < LooseVersion('3.0'):
-    raise SystemExit(
-        'ERROR: Ansible requires Jinja2 3.0 or newer on the controller. '
-        'Current version: %s' % jinja2_version
-    )
+
+# STOP THIS!!! Jinja updates used for building docs, and requires an update
+# The demanded update is not available packaged for RHEL 8 or RHEL 9
+# and never will be because Red Hat does not update system dependencies
+# satisfy the "oohhhh, shiny!!!" of fan boys <nkadel@gmail.com
+#if jinja2_version < LooseVersion('3.0'):
+#    raise SystemExit(
+#        'ERROR: Ansible requires Jinja2 3.0 or newer on the controller. '
+#        'Current version: %s' % jinja2_version
+#    )
 
 import errno
 import getpass
